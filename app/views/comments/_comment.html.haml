#comment
    .user-name
        = link_to comment.user.user_name, profile_path(comment.user.user_name)
    .comment-content
        = comment.content
    - if user_signed_in?
        - if comment.user == current_user || post.user.id == current_user.id
            = link_to post_comment_path(post,comment),title: "Eliminar Comentário", method: :delete, data: {}, remote: true do 
                %span(class="glyphicon glyphicon-remove delete-comment")